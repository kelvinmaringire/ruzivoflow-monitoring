# Copy to .env and set values (never commit .env).
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=change-me

# Alertmanager SMTP is configured in alertmanager/alertmanager.yml (smarthost, from, user, to).
# Put the SMTP password only in secrets/smtp_password (single line).
