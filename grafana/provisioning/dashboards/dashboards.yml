apiVersion: 1

providers:
  - name: "Provisioned"
    orgId: 1
    folder: "VPS"
    type: file
    disableDeletion: true
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
